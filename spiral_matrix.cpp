#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    void matrixby90(vector<vector<int>> &nums ){
        int rows=nums.size();
        int cols=nums[0].size();
        while()
}

    }
};
int main(){
    vector<vector<int>> nums={{1, 2, 3}, {4, 5, 6}, {7, 8, 9}};
    Solution obj;
    obj.matrixby90(nums);
    int rows=nums.size();
        int cols=nums[0].size();
        for(int i=0;i<rows;i++){
            for(int j=0;j<cols;j++){
                cout<<nums[i][j]<<" ";
            }
            cout<<endl;
        }

    return 0;
}